apiVersion: v1
kind: Service
metadata:
  name: grove-cms
  labels:
    app: grove-cms
spec:
  type: ClusterIP
  selector:
    app: grove-cms
  ports:
    - name: http
      port: 3001
      targetPort: 3001
